{# 实体类生成模板 #}
@Data
@Accessors(chain = true)
@TableName("{{table_name}}")
public class {{table_name}} implements Serializable {
{% for column in column_vec %}
/*
*   {{column.commentary}}
*/
@TableField("{{column.name}}")
private {{column.type_info}} {{column.name}};
{% endfor %}
}